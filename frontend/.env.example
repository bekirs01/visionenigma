# Frontend (Next.js)
# Boş bırakırsanız: next.config.js rewrites ile /api ve /health backend'e (8000) proxy edilir.
# Proxy kullanmıyorsanız: Backend'in doğrudan adresi (CORS açık olmalı)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
