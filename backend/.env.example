# Admin panele giriş için kod (zorunlu). Bu kodu /admin/login sayfasında yazacaksınız.
ADMIN_ACCESS_CODE=admin123

# İsteğe bağlı: OpenAI analiz için
# OPENAI_API_KEY=sk-...

# Gelen e-posta (INBOX) — IMAP. Gmail: imap.gmail.com:993, SMTP_USER/PASS ile aynı App Password kullanılabilir.
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# IMAP_USER=...
# IMAP_PASS=...

# İsteğe bağlı: E-posta göndermek için (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=...
# SMTP_PASS=...
# SMTP_FROM=...

# Railway Cron Job ile periyodik mail sync için (opsiyonel). Header: X-Cron-Secret
# CRON_SECRET=<rastgele-gizli-değer>
# EMAIL_SYNC_INTERVAL_SECONDS=60
