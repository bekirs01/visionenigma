# App
APP_ENV=development
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Database (PostgreSQL)
POSTGRES_DB=support_mvp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@db:5432/support_mvp

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Email (future) — default mock, no real credentials required
EMAIL_MODE=mock
IMAP_HOST=
IMAP_PORT=993
IMAP_USER=
IMAP_PASS=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# AI provider — default mock, no API key required
AI_PROVIDER=mock
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-4o-mini

# Telegram acil bildirim (negatif/acil ticket'larda tek seferlik mesaj)
# TELEGRAM_ENABLED=true
# TELEGRAM_BOT_TOKEN=<bot-token>
# TELEGRAM_CHAT_ID=<grup-veya-kisisel-chat-id>
# TELEGRAM_APP_URL=http://localhost:3000
